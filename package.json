{
  "name": "skincarerecommendationsystem",
  "version": "1.0.0",
  "type": "module",
  "description": "Full-stack Skin Care Recommendation System with React and Flask ML model.",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently -k -n MODEL,SERVER,CLIENT -c yellow,green,cyan \"npm run model\" \"npm run client\"",
    "model": "node scripts/runModel.js",
    "client": "cd ui && npm start",
    "build": "cd ui && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
